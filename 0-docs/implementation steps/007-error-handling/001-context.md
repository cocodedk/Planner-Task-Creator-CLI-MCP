# Error Handling Module Context

**Purpose**: Provide structured, machine-readable error responses for CLI.

**Scope**: Define error types, format, and handling across all modules.

**Key Requirements**:
- Structured JSON error format for programmatic consumption
- Clear error codes and human-readable messages
- Include helpful context (candidates, required scopes, etc.)
- Consistent error handling across all modules
