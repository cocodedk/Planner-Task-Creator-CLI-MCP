# CLI Commands Module Context

**Purpose**: Implement all CLI commands using Typer framework.

**Scope**: Create command handlers for add, init-auth, set-defaults, list-plans, list-buckets.

**Key Requirements**:
- Use Typer for CLI framework and argument parsing
- Rich library for colored output
- Structured JSON error output for machine readability
- Configuration resolution across CLI flags, env vars, config file
